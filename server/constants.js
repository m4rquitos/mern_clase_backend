const DB_USER = "admin"
const DB_PASSWORD = "0123456789"

const DB_HOST = "cluster0.7mv4tzq.mongodb.net"

const API_VERSION = "v1"
const IP_SERVER = "localhost"

const JWT_SECRET_KEY = 'marcos123chorizo'

module.exports = {
    DB_USER, 
    DB_PASSWORD, 
    DB_HOST,
    API_VERSION,
    IP_SERVER,
    JWT_SECRET_KEY
}
